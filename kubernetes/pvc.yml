apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: asterisk-data
  namespace: voip
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
